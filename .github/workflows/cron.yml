name: Data Refresh

on:
  workflow_dispatch:
  schedule:
    - cron: "0 * * * *"

permissions:
  contents: read

concurrency:
  group: data-refresh
  cancel-in-progress: false

jobs:
  data-refresh:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    env:
      POSTGRES_PRISMA_URL: ${{ secrets.POSTGRES_PRISMA_URL }}
      POSTGRES_URL_NON_POOLING: ${{ secrets.POSTGRES_URL_NON_POOLING }}
      BASE_RPC_URL: ${{ secrets.BASE_RPC_URL }}
      # Indexer primary RPC uses PAYG via BASE_RPC_URL (BASE_RPC_URL_INDEXER intentionally omitted).
      AGENT_INDEX_MODE: erc8004
      ERC8004_REGISTRY_ADDRESS: ${{ secrets.ERC8004_REGISTRY_ADDRESS }}
      AGENT_INDEX_CHUNK_SIZE: "1000"
      AGENT_INDEX_CHUNK_DELAY_MS: "40"
      AGENT_METADATA_CONCURRENCY: "8"
      AGENT_METADATA_BATCH_DELAY_MS: "0"
      AGENT_TOKEN_RESOLVE_TIMEOUT_MS: "5000"
      AGENT_PRISMA_RETRY_ATTEMPTS: "4"
      AGENT_PRISMA_RETRY_DELAY_MS: "1000"
      AGENT_PRISMA_OPERATION_TIMEOUT_MS: "180000"
      AGENT_PRISMA_CONNECTION_TIMEOUT_MS: "12000"
      GHOST_TELEMETRY_WINDOW_MINUTES: "1440"

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20.20.0
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Generate Prisma Client
        run: npx prisma generate

      - name: Run migration
        run: npm run migrate:indexer

      - name: Run indexer
        run: npm run index:db

      - name: Ingest telemetry signals
        run: npm run ingest:telemetry

      - name: Report index health
        run: npx tsx scripts/report-index-health.ts
